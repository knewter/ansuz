<% if @page.show_sub_menu %>
  <div id="flatChildNav">
    <h4>Child Pages:</h4>
    <ol>
      <% @page.children.each do |page| %>
        <li><a href='<%= page.name %>/'><%= page.title %></a></li>
      <% end %>
    </ol>
  </div>
<% end %>