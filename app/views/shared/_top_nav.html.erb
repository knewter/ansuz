<% @topNav.each do |page| 
  isActive = @breadcrumb && (page == @breadcrumb[1] || page == @page)
  url = "/pages/" + page.name + "/" %> 
    <li <%= "class=\"active\"" if isActive -%>>
      <a href='<%= url %>'><%= page.title -%></a>
    </li> 
<% end %> 
