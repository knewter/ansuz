<%= title "Testimonials" -%>
<% content_for :sidebar do -%>
  <%= link_to "New Testimonial", new_admin_testimonial_path, :class => 'button icon add' -%>
  <div class='note'>
    This plugin provides a helper method, "show_testimonials", which will provide a soft-fading transition between randomly-selected testimonials each time it's run (granted the appropriate styles are provided.  See the <%= link_to "ansuz_theme_isotope11 theme", "http://github.com/knewter/ansuz_theme_isotope11/tree/master" -%> for an example.)
  </div>
<% end -%>
<div class='fullwidth'>
  <table class='subdued'>
    <thead>
      <tr>
        <th>Content</th>
        <th>Attributed To</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @testimonials.each do |testimonial| -%>
        <tr class='<%= cycle('odd', 'even') -%>'>
          <td><%= testimonial.content -%></td>
          <td><%= testimonial.attributed_to -%></td>
          <td>
            <ul class='admin_actions'>
              <li><%= link_to famfamfam_icon("pencil"), edit_admin_testimonial_path(testimonial) -%></li>
              <li><%= link_to famfamfam_icon("delete"), admin_testimonial_path(testimonial), :method => :delete, :confirm => "Are you sure you want to delete this testimonial?" -%></li>
            </ul>
          </td>
        </tr>
      <% end -%>
    </tbody>
  </table>
</div>
